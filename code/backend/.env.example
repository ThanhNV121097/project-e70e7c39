# Runtime PostgreSQL connection string.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable
# HTTP listening port. APP_PORT is fallback only.
PORT=8080
# Optional fallback when PORT is unset.
APP_PORT=8080
