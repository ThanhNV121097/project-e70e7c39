# PostgreSQL connection string injected by Compose or deployment.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable
# HTTP listen port; runtime injects PORT.
PORT=8080
# Optional legacy fallback when PORT is unset.
APP_PORT=8080
