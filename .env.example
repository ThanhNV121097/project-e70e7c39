# Local PostgreSQL user.
POSTGRES_USER=app
# Local PostgreSQL password. Change outside version control.
POSTGRES_PASSWORD=app_secret
# Local PostgreSQL database name.
POSTGRES_DB=app
# Browser API URL for compose users.
NEXT_PUBLIC_API_URL=http://localhost:8080
# Frontend server API origin in compose network.
API_ORIGIN=http://backend:8080
